# public/

Static assets in this folder are copied as-is to `dist/` by Vite at build time.
They are served at the root path — e.g. `public/favicon.ico` → `/favicon.ico`.

place files here:
- favicon.ico
- icons/
- images/
